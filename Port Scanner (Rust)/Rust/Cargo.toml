[package]
name = "rust_port_scanner"
version = "4.0.0"
edition = "2021"
authors = ["CyberSecurity Portfolio"]
description = "A safe, low-level, and incredibly fast TCP port scanner."

[dependencies]
tokio = { version = "1.36", features = ["full"] }
clap = { version = "4.5", features = ["derive"] }
